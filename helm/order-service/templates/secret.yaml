apiVersion: v1
kind: Secret
metadata:
  name: {{ include "order-service.fullname" . }}
  labels:
    {{- include "order-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  db-username: {{ .Values.app.datasource.username | quote }}
  db-password: {{ .Values.app.datasource.password | quote }}
